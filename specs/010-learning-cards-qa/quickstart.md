# Quickstart: Q&A Learning Cards Page

**Date**: 2025-12-06

## Prerequisites

- Node.js (latest LTS)
- npm or yarn

## Setup

1. Clone the repository
2. Navigate to the project root
3. Install dependencies: `npm install`

## Development

- Start dev server: `npm run dev`
- Open http://localhost:5173 (or configured port)
- Navigate to the Q&A page

## Testing

- Unit tests: `npm test`
- E2E tests: `npx playwright test`
- Type check: `npx tsc --noEmit`

## Building

- Production build: `npm run build`
- Preview build: `npm run preview`

## Key Files

- Main component: `web/src/pages/QALearningCards.tsx` (to be created)
- API service: `web/src/services/qaService.ts` (to be created)
- Local storage: `web/src/services/storageService.ts` (to be created)
